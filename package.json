{
  "name": "brix-charts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "fetch": "node scripts/fetch-data.mjs",
    "serve": "npx --yes serve web"
  }
}
